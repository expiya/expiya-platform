import type { Metadata } from "next";
import { CORDLESS_DRILL_PRODUCTS } from "@/features/cordless-drill/catalog";
import { projectCordlessDrillSet } from "@/features/cordless-drill/presentation";
export const metadata:Metadata={title:"Expiya El Aletleri — Akülü Matkap Seçimi",description:"Türkiye'de satılan exact akülü matkap ve akülü darbeli matkap varyantlarını doğrulanmış teknik kanıtlarla karşılaştırın.",alternates:{canonical:"/tools"}};
export default function ToolsPage(){const set=projectCordlessDrillSet(CORDLESS_DRILL_PRODUCTS.slice(0,4).map(row=>({id:row.exactProductId,name:`${row.brand} ${row.model}`,configuration:row.configuration})));return <main className="mx-auto min-h-dvh max-w-5xl px-5 py-16"><p className="text-sm font-bold uppercase tracking-widest text-emerald-800">Expiya El Aletleri</p><h1 className="mt-4 text-5xl font-semibold">Akülü matkap karar desteği</h1><p className="mt-6 max-w-3xl text-lg text-stone-600">16 exact Türkiye varyantı; bare gövde ve kit ayrımı, batarya platformu, darbe, tork, mandren, devir ve ağırlık bağlamıyla değerlendirilir.</p><section className="mt-12 rounded-3xl border p-7"><h2 className="text-2xl font-semibold">{set.title}</h2><p className="mt-3 text-stone-600">{set.explanation}</p><ul className="mt-6 space-y-3">{set.candidates.map(row=><li key={row.id}><strong>{row.name}</strong>{row.configuration?` — ${row.configuration}`:""}</li>)}</ul><p className="mt-6 text-sm text-stone-500">Fiyat, satıcı ve puan teknik veya sıralama otoritesi değildir. Bilinmeyenler nötr kalır.</p></section></main>}
