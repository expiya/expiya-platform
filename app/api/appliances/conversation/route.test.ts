import { describe, expect, it } from "vitest";
import { POST } from "./route";
describe("Appliances conversation API authority boundary",()=>{
 it.each(["ledger","authority","pinnedSemanticDigest","revision"])("rejects client-supplied authoritative field %s",async(field)=>{const body={action:"TURN",conversationId:"11111111-1111-4111-8111-111111111111",messageId:"m",expectedRevision:1,message:"Sessiz olsun",[field]:field==="ledger"?[]:"injected"};const response=await POST(new Request("http://localhost/api/appliances/conversation",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}));expect(response.status).toBe(400);expect(await response.json()).toMatchObject({kind:"FAILED_CLOSED"});});
 it("admits the now-active countertop microwave before persistence availability is evaluated",async()=>{const response=await POST(new Request("http://localhost/api/appliances/conversation",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"CREATE",conversationId:"11111111-1111-4111-8111-111111111111",messageId:"create",productType:"COUNTERTOP_MICROWAVE_OVEN"})}));expect(response.status).toBe(503);expect(await response.json()).toMatchObject({kind:"FAILED_CLOSED"});});
 it("admits the now-active exact-pair split AC before persistence availability is evaluated",async()=>{const response=await POST(new Request("http://localhost/api/appliances/conversation",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"CREATE",conversationId:"11111111-1111-4111-8111-111111111111",messageId:"create",productType:"SPLIT_AIR_CONDITIONER"})}));expect(response.status).toBe(503);expect(await response.json()).toMatchObject({kind:"FAILED_CLOSED"});});
});
