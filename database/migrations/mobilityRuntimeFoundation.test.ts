import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("Mobility migration",()=>{const sql=readFileSync("database/migrations/0017_mobility_runtime_foundation.sql","utf8");it("is additive and guarded",()=>{expect(sql).toContain("create table if not exists mobility_conversations");expect(sql).toContain("primary key(conversation_id,message_id)");expect(sql).toContain("check(category_id in('ELECTRIC_SCOOTER','ELECTRIC_BICYCLE','BICYCLE'))");expect(sql).not.toMatch(/drop table|truncate/iu)})});
