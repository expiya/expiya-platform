import { readFileSync } from "node:fs"; import path from "node:path"; import { describe, expect, it } from "vitest";
const sql = readFileSync(path.join(process.cwd(), "database/migrations/0015_electronics_runtime_foundation.sql"), "utf8");
describe("Electronics runtime migration", () => { it("is additive, transactional and enforces category/CAS support", () => { expect(sql).toMatch(/^begin;/u); expect(sql.trimEnd()).toMatch(/commit;$/u); expect(sql).toContain("create table if not exists electronics_conversations"); expect(sql).toContain("primary key (conversation_id,message_id)"); expect(sql).toContain("ELECTRONICS"); expect(sql).not.toMatch(/drop\s|alter\s+table/iu); for (const id of ["SMARTPHONE", "GAME_CONSOLE", "SMART_HOME_HUB", "UNINTERRUPTIBLE_POWER_SUPPLY"]) expect(sql).toContain(id); }); });
