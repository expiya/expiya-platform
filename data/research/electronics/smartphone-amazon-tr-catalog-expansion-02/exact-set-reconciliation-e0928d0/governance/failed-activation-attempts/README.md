# Failed activation attempts

`ACT-01` and `ACT-02` are quarantined audit artifacts from pre-commit activation
attempts. Both attempts were rolled back to the verified Electronics v1.2 pointer
after post-activation validation failed. Neither attempt was pushed or deployed.

The only production runtime release activated by this change is
`ELECTRONICS-RUNTIME-CATALOG-TR-v1.3-SMARTPHONE-V02-RECONCILED-R2`.
