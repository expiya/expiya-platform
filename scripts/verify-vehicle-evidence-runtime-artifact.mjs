import { createHash } from "node:crypto";
import { readFile } from "node:fs/promises";
const hash = (b) => createHash("sha256").update(b).digest("hex");
const manifest = JSON.parse(await readFile("data/runtime/vehicle-evidence/v0.4.0/manifest.json", "utf8"));
const bytes = await readFile(manifest.artifactPath); const artifact = JSON.parse(bytes);
if (hash(bytes) !== manifest.artifactSha256) throw new Error("VEHICLE_EVIDENCE_ARTIFACT_HASH_MISMATCH");
if (artifact.validationStatus !== "PASS" || artifact.candidates.length < 8 || artifact.policy?.migratedCategories?.join() !== "seats,cargo_volume_l") throw new Error("VEHICLE_EVIDENCE_ARTIFACT_NOT_ACTIVATION_ELIGIBLE");
for (const key of ["runtimeVehicleCandidateId", "vehicleVariantId", "configurationId"]) if (new Set(artifact.candidates.map((x) => x[key])).size !== artifact.candidates.length) throw new Error(`DUPLICATE_${key}`);
const statuses = artifact.candidates.map((c) => c.facts.seats.status);
if (statuses.some((s) => s !== "AVAILABLE")) throw new Error("SEATS_EVALUABILITY_DRIFT");
const cargo = artifact.candidates.map((c) => c.facts.cargo_volume_l.status);
if (cargo.some((s) => s !== "AVAILABLE")) throw new Error("CARGO_EVALUABILITY_DRIFT");
const captur = artifact.candidates.find((c) => c.runtimeVehicleCandidateId === "RVC-PILOT-0002")?.facts.cargo_volume_l;
if (captur?.valueMin !== 484 || captur?.valueMax !== 616 || captur?.rangeSemantics !== "MIN_MAX") throw new Error("CARGO_RANGE_DRIFT");
console.log(`Vehicle Evidence runtime artifact verified: ${artifact.artifactVersion} ${hash(bytes)}; mappings=${artifact.candidates.length} complete=${artifact.candidates.length}`);
