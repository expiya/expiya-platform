import type { ElectronicsCategoryId } from "./architectureBaseline";
export type ElectronicsOutcomeKind = "ASK" | "CLARIFY" | "RESPOND" | "DECISION_READY" | "UNSUPPORTED" | "FAILED_CLOSED";
export interface ElectronicsLedgerEvent { readonly eventId: string; readonly conceptId: string; readonly normalizedValue: unknown; readonly sourceMessageId: string; readonly status: "ACCEPTED_EXPLICIT" | "SUPERSEDED" | "CLEARED" | "REJECTED"; readonly authority: "USER_EXPLICIT"; readonly decisionUse: "HARD_FILTER" | "QUESTION_INPUT" | "NONE"; readonly supersedesEventId?: string; readonly createdRevision: number; readonly createdAt: string }
export interface ElectronicsDecisionCard { readonly exactProductId: string; readonly categoryId: ElectronicsCategoryId; readonly manufacturer: string; readonly modelCode: string; readonly configurationIdentity: string; readonly rationale: readonly { readonly acceptedConcept: string; readonly evidenceFactIds: readonly string[]; readonly explanationTr: string }[]; readonly technicalEvidence: readonly { readonly label: string; readonly value: string }[]; readonly dailyLifeInterpretation: readonly string[]; readonly authorizationFingerprint: string; readonly authority: { readonly policyDigest: string; readonly catalogReleaseDigest: string; readonly contextRevision: number } }
export interface ElectronicsRuntimeOutcome { readonly kind: ElectronicsOutcomeKind; readonly message: string; readonly questionKey?: string; readonly choices?: readonly { readonly value: string; readonly label: string }[]; readonly contextRevision?: number; readonly candidateSummaries?: readonly { readonly manufacturer: string; readonly modelCode: string }[]; readonly uncertainty?: readonly string[]; readonly card?: ElectronicsDecisionCard; readonly decisionFingerprint?: string; readonly budgetEligibility?: "NOT_ENABLED" | "ELIGIBLE" | "INELIGIBLE" | "BUDGET_ELIGIBILITY_UNKNOWN" }
export interface ElectronicsConversationState { readonly conversationId: string; readonly schemaVersion: "electronics-conversation/v1"; readonly revision: number; readonly departmentId: "ELECTRONICS"; readonly categoryId: ElectronicsCategoryId; readonly pinnedCatalogRelease: string; readonly pinnedCatalogDigest: string; readonly pinnedPolicyVersion: string; readonly pinnedPolicyDigest: string; readonly ledger: readonly ElectronicsLedgerEvent[]; readonly askedQuestionKeys: readonly string[]; readonly lastQuestionKey?: string; readonly pendingQuestionKey?: string; readonly personaSignals: readonly { readonly signalId: string; readonly authority: "DERIVED_PLANNING"; readonly decisionUse: "NONE"; readonly directCandidateEffect: "NONE" }[]; readonly budget?: { readonly mode: "NEEDS_ONLY" | "BUDGET_AS_DECISION_FILTER"; readonly maximumTry?: number; readonly sourceMessageId?: string }; readonly decisionRecord?: ElectronicsDecisionCard; readonly ended: boolean; readonly createdAt: string; readonly updatedAt: string }
export interface ElectronicsStoredOutcome { readonly kind: "CONVERSATION_BOOTSTRAPPED" | "CONTEXT_MUTATED"; readonly conversationId: string; readonly revision: number; readonly state: ElectronicsConversationState; readonly publicOutcome?: ElectronicsRuntimeOutcome }
export interface ElectronicsStoredMessage { readonly messageId: string; readonly payloadHash: string; readonly committedRevision: number; readonly outcome: ElectronicsStoredOutcome }
