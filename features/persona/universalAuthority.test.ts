import { describe, expect, it } from "vitest";
import { personaCatalogReadiness, personaDigest, rankWithApprovedPersona, UNIVERSAL_PERSONA_SCHEMA_VERSION, validateUniversalPersonaPackage, type UniversalPersonaPackage } from "./universalAuthority";

const base = (): UniversalPersonaPackage => { const value = { schemaVersion: UNIVERSAL_PERSONA_SCHEMA_VERSION, releaseId: "test/v1", generatedAt: "2026-09-06T00:00:00.000Z", inventoryDigest: `sha256:${"a".repeat(64)}` as const, vocabulary: [], methodology: { singleSourceAuthority: false as const, manufacturerRole: "POSITIONING_ONLY" as const, technicalFactsCanInferPersona: false as const, userThemeMinimum: { minimumReviews: 20, minimumPlatforms: 2, minimumIndependentAuthors: 20, personalData: "EXCLUDED" as const }, foreignMarketRule: "EXACT_FAMILY_COMPATIBILITY_AND_EXPLICIT_LIMITATION_REQUIRED" as const, sourceCountGaming: "FORBIDDEN" as const }, scoringPolicy: { status: "PROPOSED_OWNER_APPROVAL_REQUIRED" as const, formula: "BASE_SCORE_PLUS_CAPPED_PERSONA" as const, decisionUse: "BOUNDED_SOFT_RANKING_ONLY" as const, eligibleContributions: [0.25,0.5,0.75], candidateCap: 0.75 as const, membershipEffect: "NONE" as const, unknownEffect: 0 as const, conflictedEffect: 0 as const, inactiveEffect: 0 as const, tiePolicy: "TIES_REMAIN_TIES" as const, serializationOrderAuthority: false as const }, sources: [], claims: [], records: [{ exactProductId:"p", departmentId:"APPLIANCES" as const, categoryId:"AIR_FRYER", brand:"b", model:"m", configurationIdentity:"c", status:"PERSONA_EVIDENCE_UNKNOWN" as const, traits:[], claimIds:[], unknownReason:"INSUFFICIENT_INDEPENDENT_CORROBORATION", conflictIds:[] }], conflicts:[], ownerApproval:{state:"PENDING" as const, requiredBindings:["VOCABULARY","EVIDENCE_RULES","EXPRESSION_MAPPINGS","SCORING_POLICY","EXACT_PRODUCT_RECORDS"], approvalId:null, approvedPayloadDigest:null}, payloadDigest:`sha256:${"0".repeat(64)}` as const }; const vocabulary=["ADVENTURE","CHARISMATIC","COMFORT","COMPACT_FEELING","DESIGN_LED","DRIVING_ENGAGEMENT","FAMILY_ORIENTED","PLAYFUL","PRACTICALITY","PREMIUM_POSITIONING","PROFESSIONAL","TECHNOLOGY","UNDERSTATED","VISUALLY_DISTINCTIVE"].map(trait=>({trait,meaning:trait,turkishExpressions:[trait],aliases:[]})); return { ...value, vocabulary, payloadDigest: personaDigest({...value,vocabulary,payloadDigest:undefined}) } as unknown as UniversalPersonaPackage; };
describe("universal persona authority",()=>{
  it("requires exact coverage and owner approval",()=>{const p=base();expect(validateUniversalPersonaPackage(p,[{exactProductId:"p",departmentId:"APPLIANCES",categoryId:"AIR_FRYER"}])).toEqual([]);expect(personaCatalogReadiness({package:p,expectedExactProductIds:["p"]})).toEqual({status:"CATALOG_NOT_READY",reason:"PERSONA_OWNER_APPROVAL_REQUIRED"});expect(personaCatalogReadiness({package:p,expectedExactProductIds:["p","q"]})).toEqual({status:"CATALOG_NOT_READY",reason:"PERSONA_COVERAGE_INCOMPLETE"});});
  it("cannot rank before approval",()=>expect(()=>rankWithApprovedPersona({package:base(),eligibleCandidateIds:["p"],preferences:[],singleSelectionAuthorized:false})).toThrow("PERSONA_OWNER_APPROVAL_REQUIRED"));
});
