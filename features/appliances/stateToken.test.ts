import { describe, expect, it } from "vitest"; import { createAppliancesRecoveryTokenService } from "./stateToken.server";
describe("Appliances recovery token", () => { it("carries only identity and expected revision", () => { const service = createAppliancesRecoveryTokenService({ secret: "x".repeat(32), now: () => new Date("2026-09-03T00:00:00Z") }); const token = service.issue("c", 3); expect(service.verify(token, "c")).toMatchObject({ status: "VALID", payload: { conversationId: "c", expectedRevision: 3 } }); expect(token).not.toContain("ledger"); }); it("rejects tampered and expired tokens", () => { const issue = createAppliancesRecoveryTokenService({ secret: "x".repeat(32), now: () => new Date("2026-09-03T00:00:00Z") }); const token = issue.issue("c", 0, 1000); expect(issue.verify(`${token}x`, "c").status).toBe("INVALID"); const verify = createAppliancesRecoveryTokenService({ secret: "x".repeat(32), now: () => new Date("2026-09-03T00:00:02Z") }); expect(verify.verify(token, "c").status).toBe("INVALID"); }); });

