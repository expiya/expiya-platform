# Amazon Creators API offline fixtures

These fixtures contain synthetic contract data only—no credentials, copied Amazon media bytes, or claims of current availability. They exercise the same parser, exact ASIN + structured manufacturer-model gate, expiry, and affiliate-link projection used by the server adapter.
