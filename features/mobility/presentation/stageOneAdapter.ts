import { defineXpyStageOnePresentationAdapter, naturalConsumerConfiguration, XPY_STAGE_ONE_PRESENTATION_VERSION, type XpyStageOneSetPresentation } from "@/features/xpy/stageOnePresentation";
import { MOBILITY_SOURCES } from "../catalog";
import type { authorizeMobilityCard } from "../decision";
type Card=ReturnType<typeof authorizeMobilityCard>;const display=(v:unknown)=>v==="UNKNOWN"?"Bilinmiyor":String(v);
export const MOBILITY_STAGE_ONE_PRESENTATION=defineXpyStageOnePresentationAdapter<Card>({adapterId:"mobility-stage1-presentation/v1",version:XPY_STAGE_ONE_PRESENTATION_VERSION,project(card){const sources=MOBILITY_SOURCES.filter(s=>card.evidenceIds.includes(s.id));return{schemaVersion:XPY_STAGE_ONE_PRESENTATION_VERSION,exactIdentity:{id:card.exactProductId,brand:card.brand,model:card.model,configuration:naturalConsumerConfiguration(card.configurationIdentity,card.brand,card.model)},media:{status:"UNAVAILABLE",alt:`${card.brand} ${card.model} ürün görseli`},badge:"Mobilite · Doğrulanmış karar sonucu",reasons:["Belirttiğiniz koşullar ve açık seçiminizle yetkilendirildi."],matchedNeeds:[],supportingContext:[],technicalFacts:Object.entries(card.facts).slice(0,5).map(([label,value])=>({label,value:display(value),explanation:"Üretici veya exact Türkiye ürün kaydındaki değer; UNKNOWN ise tahmin edilmez."})),capabilities:card.capabilities.map(label=>({label})),limitations:card.limitations,offers:[],commerceNotice:"Fiyat ve stok teknik uygunluktan ayrıdır; güncel exact teklif doğrulanmadan bütçe filtresi uygulanmaz.",sources:sources.map(s=>({label:s.authority,href:s.url})),audit:{authorizationFingerprint:card.authorizationFingerprint,catalogDigest:card.catalogDigest,contextRevision:card.contextRevision}}}});
export function projectMobilitySet(category:string,candidates:readonly{id:string;label:string}[]):XpyStageOneSetPresentation{return{schemaVersion:XPY_STAGE_ONE_PRESENTATION_VERSION,kind:"NON_DOMINATED_SET",departmentLabel:"Mobilite",categoryLabel:category,title:"Tek bir seçenek üstün değil",explanation:"Doğrulanmış bilgiler farklı kullanım önceliklerine işaret ediyor; marka, fiyat veya katalog sırası kazanan üretmedi.",candidates:candidates.map(x=>({id:x.id,name:x.label})),unresolved:["Kullanım, taşıma, arazi veya fit gereksiniminizi netleştirin ya da bir modeli açıkça seçin."]}}
