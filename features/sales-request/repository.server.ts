import type { ConsentReceipt, DealerTransferEnvelope, SalesRequestInput } from "./contracts";

export type StoredSalesRequest = { readonly requestId: string; readonly createdAt: string; readonly binding: { readonly conversationId: string; readonly decisionRevision: number; readonly decisionFingerprint: string; readonly offerId: string; readonly exactVariantId: string; readonly configurationIdentity: string; readonly catalogRelease: string; readonly catalogFingerprint: string; readonly stageThreeAuthorityFingerprint: string; readonly intent: string }; readonly phoneVerification: { readonly verifiedAt: string; readonly method: "SMS_OTP" }; readonly conversationSummary: { readonly text: string; readonly checksum: string } | null; readonly contact: Omit<SalesRequestInput, "handoff" | "csrfToken" | "idempotencyKey" | "phoneVerificationToken" | "conversationSummaryChecksum" | "version">; readonly consents: readonly ConsentReceipt[]; readonly outbound: DealerTransferEnvelope };
export interface SalesRequestRepository { findByIdempotencyKey(key: string): Promise<StoredSalesRequest | undefined>; save(key: string, request: StoredSalesRequest): Promise<void>; }
export class InMemorySalesRequestRepository implements SalesRequestRepository { private readonly records = new Map<string, StoredSalesRequest>(); async findByIdempotencyKey(key: string) { return this.records.get(key); } async save(key: string, request: StoredSalesRequest) { this.records.set(key, request); } clear() { this.records.clear(); } }
export const salesRequestRepository = new InMemorySalesRequestRepository();
