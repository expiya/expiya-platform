import { beforeEach, describe, expect, it } from "vitest";
import { appendSalesAdvisorHistory, resetSalesAdvisorHistoryForTests, salesAdvisorHistoryKey } from "@/features/sales-advisor/history.server";
import { buildShareableSalesSummary } from "./salesSummary.server";
const handoff={version:"phase3-intent/v1",conversationId:"c",decisionFingerprint:"d",offerId:"o",selectedExactVariantId:"v",catalogRelease:"r",intent:"REQUEST_QUOTE",nonce:"test_nonce_123456",issuedAt:"2099-01-01T00:00:00Z",expiresAt:"2099-01-01T00:30:00Z",executionAuthorized:false,approvedNeeds:[{concept:"primaryUsage",summary:"Ana kullanım: şehir içi"}]} as const;
describe("user-visible sales conversation summary",()=>{beforeEach(resetSalesAdvisorHistoryForTests);it("uses confirmed phase 1 needs and phase 2 user questions, not assistant answers",()=>{appendSalesAdvisorHistory(salesAdvisorHistoryKey("c","o","v"),[{role:"user",text:"Bagajı kaç litre?"},{role:"assistant",text:"Gizli veya gereksiz cevap"}],handoff.expiresAt);const summary=buildShareableSalesSummary(handoff);expect(summary.text).toContain("Ana kullanım: şehir içi");expect(summary.text).toContain("Bagajı kaç litre?");expect(summary.text).not.toContain("Gizli veya gereksiz cevap");});it("drops sensitive phase 2 messages",()=>{appendSalesAdvisorHistory(salesAdvisorHistoryKey("c","o","v"),[{role:"user",text:"TC kimlik 12345678901"}],handoff.expiresAt);expect(buildShareableSalesSummary(handoff).text).not.toContain("12345678901");});});
