export const usedCarsDataQualityReadinessSnapshot = Object.freeze({ prerequisites: Object.freeze({ qualityThresholdsReady: true, correctionWorkflowReady: true, stagingJobManifestReady: true, stagingRehearsalMatrixReady: true, stagingDashboardGateReady: true, priceFreshnessJobConfigured: false, stockFreshnessJobConfigured: false, duplicateDetectionCalibrated: false, taxonomyReferenceMonitorConfigured: false, evidenceConflictQueueStaffed: false, soldListingRemovalSlaTested: false, qualityDashboardReviewed: false, correctionRehearsalPassed: false }), publicInventoryMonitoringAuthorized: false as const, automaticRepublishAuthorized: false as const, qualityWaiverAuthorized: false as const });
export function assessDataQualityReadiness() { const missing = Object.entries(usedCarsDataQualityReadinessSnapshot.prerequisites).filter(([, ready]) => !ready).map(([key]) => key); return Object.freeze({ ready: missing.length === 0, missing: Object.freeze(missing), publicInventoryMonitoringAuthorized: false as const, automaticRepublishAuthorized: false as const, qualityWaiverAuthorized: false as const }); }
export const currentUsedCarsDataQualityReadiness = assessDataQualityReadiness();
