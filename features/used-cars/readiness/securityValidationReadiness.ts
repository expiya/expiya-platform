import { usedCarsSecurityTestPlan, validateSecurityTestPlan } from "../security-testing/testPlan";
export const usedCarsSecurityValidationReadinessSnapshot = Object.freeze({ prerequisites: Object.freeze({ adversarialTestPlanReady: validateSecurityTestPlan(usedCarsSecurityTestPlan).length === 0, findingLifecycleReady: true, isolatedTestEnvironmentReady: false, independentPentestContracted: false, allScenariosPassed: false, criticalHighFindingsClosed: false, mediumFindingsHaveOwners: false, retestEvidenceApproved: false, securitySignoffRecorded: false }), productionSecurityApprovalAuthorized: false as const, pentestAgainstProductionAuthorized: false as const, automaticRiskAcceptanceAuthorized: false as const });
export function assessSecurityValidationReadiness() { const missing = Object.entries(usedCarsSecurityValidationReadinessSnapshot.prerequisites).filter(([, ready]) => !ready).map(([key]) => key); return Object.freeze({ ready: missing.length === 0, missing: Object.freeze(missing), productionSecurityApprovalAuthorized: false as const, pentestAgainstProductionAuthorized: false as const, automaticRiskAcceptanceAuthorized: false as const }); }
export const currentUsedCarsSecurityValidationReadiness = assessSecurityValidationReadiness();
