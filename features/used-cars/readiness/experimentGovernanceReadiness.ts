export const usedCarsExperimentGovernanceReadinessSnapshot = Object.freeze({ prerequisites: Object.freeze({ experimentContractReady: true, safetyGuardrailsReady: true, allocationServicePrivacyReviewed: false, experimentationPlatformIsolated: false, fairnessReviewProcessStaffed: false, accessibilityMonitoringConnected: false, killSwitchTested: false, auditExportTested: false, independentExperimentReviewComplete: false }), realExperimentAuthorized: false as const, automaticWinnerRolloutAuthorized: false as const, organicRankingExperimentAuthorized: false as const });
export function assessExperimentGovernanceReadiness() { const missing = Object.entries(usedCarsExperimentGovernanceReadinessSnapshot.prerequisites).filter(([, ready]) => !ready).map(([key]) => key); return Object.freeze({ ready: missing.length === 0, missing: Object.freeze(missing), realExperimentAuthorized: false as const, automaticWinnerRolloutAuthorized: false as const, organicRankingExperimentAuthorized: false as const }); }
export const currentUsedCarsExperimentGovernanceReadiness = assessExperimentGovernanceReadiness();
