import { usedCarsTrustLanguage, validateTrustLanguageRegistry } from "../content/trustLanguage";
export const usedCarsContentGovernanceReadinessSnapshot = Object.freeze({ prerequisites: Object.freeze({ trustLanguageRegistryReady: validateTrustLanguageRegistry(usedCarsTrustLanguage).length === 0, publicCopyGateReady: true, contentReviewContractReady: true, legalCopyReviewComplete: false, accessibilityCopyReviewComplete: false, evidenceCopyReviewComplete: false, classicClaimCopyReviewed: false, sponsoredLabelCopyReviewed: false, productionContentInventoryApproved: false }), publicCopyPublicationAuthorized: false as const, automatedCopyPublicationAuthorized: false as const });
export function assessContentGovernanceReadiness() { const missing = Object.entries(usedCarsContentGovernanceReadinessSnapshot.prerequisites).filter(([, ready]) => !ready).map(([key]) => key); return Object.freeze({ ready: missing.length === 0, missing: Object.freeze(missing), publicCopyPublicationAuthorized: false as const, automatedCopyPublicationAuthorized: false as const }); }
export const currentUsedCarsContentGovernanceReadiness = assessContentGovernanceReadiness();
