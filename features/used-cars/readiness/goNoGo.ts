import type {LaunchStage} from "./launchControl";
export type KillSwitchScope="PUBLIC_LISTING"|"PARTNER_WRITES"|"LEAD_HANDOFF"|"PAYMENTS"|"CHANNELS"|"AI_AGENT"|"TAXONOMY_RELEASE"|"AUTH_SESSIONS"|"MEDIA_PUBLICATION"|"FEED_WEBHOOK"|"EXPORTS"|"SIGNED_URLS"|"DEALER_CLOSURE";
export const requiredKillSwitches:readonly KillSwitchScope[]=Object.freeze(["PUBLIC_LISTING","PARTNER_WRITES","LEAD_HANDOFF","PAYMENTS","CHANNELS","AI_AGENT","TAXONOMY_RELEASE","AUTH_SESSIONS","MEDIA_PUBLICATION","FEED_WEBHOOK","EXPORTS","SIGNED_URLS","DEALER_CLOSURE"]);
export interface GoNoGoInput {readonly stage:LaunchStage;readonly readinessPassed:boolean;readonly evidenceComplete:boolean;readonly rollbackDrilled:boolean;readonly observabilityGreen:boolean;readonly supportAndIncidentStaffed:boolean;readonly killSwitchesTested:readonly KillSwitchScope[];readonly unresolvedSev1OrSev2:number;readonly namedDecisionMakerId:string|null;readonly explicitGoDecision:boolean}
export function evaluateGoNoGo(input:GoNoGoInput){const blockers:string[]=[];if(!input.readinessPassed)blockers.push("READINESS_FAILED");if(!input.evidenceComplete)blockers.push("EVIDENCE_INCOMPLETE");if(!input.rollbackDrilled)blockers.push("ROLLBACK_NOT_DRILLED");if(!input.observabilityGreen)blockers.push("OBSERVABILITY_NOT_GREEN");if(!input.supportAndIncidentStaffed)blockers.push("OPERATIONS_NOT_STAFFED");for(const required of requiredKillSwitches)if(!input.killSwitchesTested.includes(required))blockers.push(`KILL_SWITCH_UNTESTED:${required}`);if(input.unresolvedSev1OrSev2>0)blockers.push("HIGH_SEVERITY_INCIDENT_OPEN");if(!input.namedDecisionMakerId)blockers.push("DECISION_MAKER_REQUIRED");if(!input.explicitGoDecision)blockers.push("EXPLICIT_GO_REQUIRED");return Object.freeze({decision:blockers.length===0?"GO" as const:"NO_GO" as const,blockers:Object.freeze(blockers),automaticDeploymentAuthorized:false as const});}
