export const usedCarsSupplyChainReadinessSnapshot = Object.freeze({ prerequisites: Object.freeze({ dependencyPolicyReady: true, artifactAttestationContractReady: true, secretInventoryContractReady: true, ciSecurityScansConfigured: false, sbomGenerationVerified: false, provenanceSigningConfigured: false, artifactRegistryImmutabilityTested: false, dependencyLicenseReviewComplete: false, secretInventoryAndRotationApproved: false, reproducibleBuildVerified: false, promotionVerificationTested: false }), productionArtifactPromotionAuthorized: false as const, secretActivationAuthorized: false as const, dependencyExceptionAuthorized: false as const });
export function assessSupplyChainReadiness() { const missing = Object.entries(usedCarsSupplyChainReadinessSnapshot.prerequisites).filter(([, ready]) => !ready).map(([key]) => key); return Object.freeze({ ready: missing.length === 0, missing: Object.freeze(missing), productionArtifactPromotionAuthorized: false as const, secretActivationAuthorized: false as const, dependencyExceptionAuthorized: false as const }); }
export const currentUsedCarsSupplyChainReadiness = assessSupplyChainReadiness();
