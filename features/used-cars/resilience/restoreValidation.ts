export interface RestoreManifest {readonly backupId:string;readonly dataClass:string;readonly environment:"STAGING"|"PRODUCTION";readonly sourceChecksum:string;readonly restoredChecksum:string;readonly tenantScope:string|null;readonly kmsKeyVersion:string;readonly auditChainVerified:boolean;readonly rowCountExpected:number;readonly rowCountRestored:number;readonly crossTenantNegativeTestPassed:boolean;readonly restoredAt:string;readonly approvedBy:string;readonly secondApproverId:string|null;readonly productionCutoverAuthorized:false}
export function validateRestoreManifest(manifest:RestoreManifest):readonly string[]{const codes:string[]=[];if(!/^sha256:[a-f0-9]{64}$/u.test(manifest.sourceChecksum)||manifest.sourceChecksum!==manifest.restoredChecksum)codes.push("CHECKSUM_MISMATCH");if(manifest.rowCountExpected!==manifest.rowCountRestored)codes.push("ROW_COUNT_MISMATCH");if(!manifest.auditChainVerified)codes.push("AUDIT_CHAIN_INVALID");if(!manifest.crossTenantNegativeTestPassed)codes.push("TENANT_ISOLATION_TEST_REQUIRED");if(!manifest.kmsKeyVersion)codes.push("KMS_KEY_VERSION_REQUIRED");if(!manifest.approvedBy||!manifest.secondApproverId)codes.push("TWO_PERSON_APPROVAL_REQUIRED");if(manifest.approvedBy===manifest.secondApproverId)codes.push("SELF_APPROVAL_FORBIDDEN");if(manifest.productionCutoverAuthorized!==false)codes.push("CUTOVER_AUTHORIZATION_FORBIDDEN");return Object.freeze(codes);}
