export const requiredPublicProjectionColumns = Object.freeze(["listing_id", "inventory_unit_id", "taxonomy_variant_id", "stock_number", "model_year", "mileage_km", "color", "asking_price_try", "damage_disclosure", "maintenance_history", "warranty", "inspection", "trust", "media_urls", "seller_display_name", "branch_display_name", "description"] as const);
export const forbiddenPublicProjectionColumns = Object.freeze(["tenant_id", "branch_id", "vin", "vin_ciphertext", "vin_fingerprint", "masked_vin", "plate", "plate_ciphertext", "plate_fingerprint", "masked_plate", "document_object_key", "contact_ciphertext", "source_reference_ids", "asserted_by"] as const);
export interface PublicProjectionIntrospection { readonly viewName: string; readonly columns: readonly string[]; readonly ownerRole: string; readonly securityBarrier: boolean; readonly publicReaderHasBaseTableGrants: boolean; readonly suspendedTenantRowCount: number; readonly expiredListingRowCount: number }
export function validatePublicProjectionIntrospection(input: PublicProjectionIntrospection) { const codes: string[] = []; if (input.viewName !== "used_cars.public_listing_projection") codes.push("CANONICAL_VIEW_REQUIRED"); if (requiredPublicProjectionColumns.some((column) => !input.columns.includes(column))) codes.push("REQUIRED_COLUMN_MISSING"); if (input.columns.some((column) => forbiddenPublicProjectionColumns.includes(column as typeof forbiddenPublicProjectionColumns[number]))) codes.push("FORBIDDEN_COLUMN_EXPOSED"); if (input.ownerRole === "PUBLIC_READER") codes.push("PUBLIC_READER_CANNOT_OWN_VIEW"); if (!input.securityBarrier) codes.push("SECURITY_BARRIER_REQUIRED"); if (input.publicReaderHasBaseTableGrants) codes.push("BASE_TABLE_GRANT_FORBIDDEN"); if (input.suspendedTenantRowCount !== 0 || input.expiredListingRowCount !== 0) codes.push("FAIL_CLOSED_FILTER_REQUIRED"); return Object.freeze({ valid: codes.length === 0, codes: Object.freeze(codes), publicGrantAuthorized: false as const }); }
