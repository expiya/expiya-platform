export interface SyntheticSeedManifest { readonly seedId: string; readonly datasetChecksum: string; readonly tenantCount: number; readonly branchesPerTenant: number; readonly usersPerTenant: number; readonly inventoryPerTenant: number; readonly allIdentifiersReservedSynthetic: true; readonly realNamesIncluded: false; readonly realContactDataIncluded: false; readonly copiedProductionRows: 0; readonly outboundDeliveryDisabled: true; readonly resetScriptChecksum: string; readonly expiresAt: string }
export function validateSyntheticSeedManifest(seed: SyntheticSeedManifest, now: string) { const codes: string[] = []; const checksum = /^sha256:[a-f0-9]{64}$/u; if (!checksum.test(seed.datasetChecksum) || !checksum.test(seed.resetScriptChecksum)) codes.push("SEED_AND_RESET_CHECKSUM_REQUIRED"); if (seed.tenantCount < 2) codes.push("CROSS_TENANT_TEST_DATA_REQUIRED"); if (seed.branchesPerTenant < 2) codes.push("CROSS_BRANCH_TEST_DATA_REQUIRED"); if (seed.usersPerTenant < 2 || seed.inventoryPerTenant < 2) codes.push("ROLE_AND_INVENTORY_TEST_DATA_REQUIRED"); if (!seed.allIdentifiersReservedSynthetic || seed.realNamesIncluded || seed.realContactDataIncluded || seed.copiedProductionRows !== 0) codes.push("REAL_DATA_FORBIDDEN"); if (!seed.outboundDeliveryDisabled) codes.push("OUTBOUND_DELIVERY_MUST_BE_DISABLED"); if (seed.expiresAt <= now) codes.push("SEED_MANIFEST_EXPIRED"); return Object.freeze({ valid: codes.length === 0, codes: Object.freeze(codes), seedExecutionAuthorized: false as const }); }
