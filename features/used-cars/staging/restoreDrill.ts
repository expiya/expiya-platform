export type RestoreDrillScenario = "FULL_SYNTHETIC_RESTORE" | "TENANT_ISOLATION_AFTER_RESTORE" | "DELETION_SUPPRESSION_REAPPLY" | "AUDIT_CHAIN_CONTINUITY" | "KMS_KEY_VERSION_RECOVERY" | "BACKUP_EXPIRY_DESTRUCTION" | "FAILOVER_FAILBACK";
export interface StagingRestoreDrillResult { readonly scenario: RestoreDrillScenario; readonly passed: boolean; readonly sourceBackupSyntheticOnly: true; readonly sourceChecksum: string; readonly restoredChecksum: string; readonly primaryReviewerId: string; readonly secondReviewerId: string; readonly evidenceChecksum: string; readonly startedAt: string; readonly completedAt: string; readonly productionCutoverPerformed: false }
export const requiredStagingRestoreDrills: readonly RestoreDrillScenario[] = Object.freeze(["FULL_SYNTHETIC_RESTORE", "TENANT_ISOLATION_AFTER_RESTORE", "DELETION_SUPPRESSION_REAPPLY", "AUDIT_CHAIN_CONTINUITY", "KMS_KEY_VERSION_RECOVERY", "BACKUP_EXPIRY_DESTRUCTION", "FAILOVER_FAILBACK"]);
export function assessStagingRestoreDrills(results: readonly StagingRestoreDrillResult[]) { const checksum = /^sha256:[a-f0-9]{64}$/u; const missing = requiredStagingRestoreDrills.filter((scenario) => !results.some((result) => result.scenario === scenario && result.passed && result.sourceBackupSyntheticOnly && result.sourceChecksum === result.restoredChecksum && checksum.test(result.sourceChecksum) && checksum.test(result.evidenceChecksum) && result.primaryReviewerId !== result.secondReviewerId && result.completedAt > result.startedAt && !result.productionCutoverPerformed)); return Object.freeze({ complete: missing.length === 0, missing: Object.freeze(missing), productionRestoreAuthorized: false as const, automaticFailoverAuthorized: false as const }); }
