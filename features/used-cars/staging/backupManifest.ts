import type { BackupDataClass } from "../resilience/backupPolicy";
export interface StagingBackupTarget { readonly dataClass: BackupDataClass; readonly providerRef: string | null; readonly kmsKeyRef: string | null; readonly region: string | null; readonly scheduleRef: string | null; readonly immutable: boolean; readonly expiryJobRef: string | null; readonly productionSourceAllowed: false; readonly backupEnabled: false }
export const usedCarsStagingBackupTargets: readonly StagingBackupTarget[] = Object.freeze([
  "TENANT_OPERATIONAL", "SENSITIVE_IDENTIFIERS", "MEDIA_DOCUMENTS", "TAXONOMY_RELEASES", "AUDIT_CHAIN", "CONFIGURATION",
].map((dataClass) => ({ dataClass: dataClass as BackupDataClass, providerRef: null, kmsKeyRef: null, region: null, scheduleRef: null, immutable: dataClass !== "CONFIGURATION", expiryJobRef: null, productionSourceAllowed: false as const, backupEnabled: false as const })));
export function validateStagingBackupTargets(targets: readonly StagingBackupTarget[]) { const codes: string[] = []; if (new Set(targets.map((item) => item.dataClass)).size !== 6 || targets.length !== 6) codes.push("DATA_CLASS_COVERAGE_REQUIRED"); if (targets.some((item) => item.productionSourceAllowed || item.backupEnabled)) codes.push("BACKUP_ENABLEMENT_FORBIDDEN"); const configured = targets.filter((item) => item.providerRef || item.kmsKeyRef || item.region || item.scheduleRef || item.expiryJobRef); if (configured.some((item) => !item.providerRef || !item.kmsKeyRef || !item.region || !item.scheduleRef || !item.expiryJobRef)) codes.push("PARTIAL_BACKUP_CONFIGURATION_FORBIDDEN"); if (targets.some((item) => item.dataClass !== "CONFIGURATION" && !item.immutable)) codes.push("IMMUTABILITY_REQUIRED"); return Object.freeze({ valid: codes.length === 0, codes: Object.freeze(codes), backupEnablementAuthorized: false as const }); }
