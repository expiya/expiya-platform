export type DataQualityRehearsalScenario = "STALE_PRICE" | "STALE_STOCK" | "DUPLICATE_VIN_CANDIDATE" | "INVALID_TAXONOMY" | "EVIDENCE_CONFLICT" | "FALSE_VERIFIED_CLAIM" | "SOLD_STILL_PUBLIC" | "CORRECTION_NEW_REVISION" | "TWO_PERSON_REVIEW";
export const requiredDataQualityRehearsals: readonly DataQualityRehearsalScenario[] = Object.freeze(["STALE_PRICE", "STALE_STOCK", "DUPLICATE_VIN_CANDIDATE", "INVALID_TAXONOMY", "EVIDENCE_CONFLICT", "FALSE_VERIFIED_CLAIM", "SOLD_STILL_PUBLIC", "CORRECTION_NEW_REVISION", "TWO_PERSON_REVIEW"]);
export interface DataQualityRehearsalResult { readonly scenario: DataQualityRehearsalScenario; readonly syntheticOnly: true; readonly detected: boolean; readonly publicationFailedClosed: boolean; readonly correctedRevisionCreated: boolean | null; readonly reviewerIds: readonly string[]; readonly evidenceChecksum: string; readonly findingsClosed: boolean }
export function assessDataQualityRehearsal(results: readonly DataQualityRehearsalResult[]) {
  const missing = requiredDataQualityRehearsals.filter((scenario) => !results.some((result) => result.scenario === scenario && result.syntheticOnly && result.detected && result.publicationFailedClosed && (scenario !== "CORRECTION_NEW_REVISION" || result.correctedRevisionCreated === true) && (scenario !== "TWO_PERSON_REVIEW" || new Set(result.reviewerIds).size >= 2) && /^sha256:[a-f0-9]{64}$/u.test(result.evidenceChecksum) && result.findingsClosed));
  return Object.freeze({ complete: missing.length === 0, missing: Object.freeze(missing), automaticRepublishAuthorized: false as const, productionMonitoringAuthorized: false as const });
}
