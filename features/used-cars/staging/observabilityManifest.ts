export type StagingTelemetryPipeline = "PUBLIC_PRODUCT" | "SPONSORED_PRODUCT" | "PARTNER_OPERATIONS" | "TRUST_OPERATIONS" | "SECURITY_SIEM";
export interface StagingTelemetrySink { readonly pipeline: StagingTelemetryPipeline; readonly endpointRef: string | null; readonly credentialRef: string | null; readonly retentionDays: number | null; readonly region: string | null; readonly rawPayloadAccepted: false; readonly productionEventsAccepted: false; readonly exportEnabled: false }
export const usedCarsStagingTelemetrySinks: readonly StagingTelemetrySink[] = Object.freeze([
  "PUBLIC_PRODUCT", "SPONSORED_PRODUCT", "PARTNER_OPERATIONS", "TRUST_OPERATIONS", "SECURITY_SIEM",
].map((pipeline) => ({ pipeline: pipeline as StagingTelemetryPipeline, endpointRef: null, credentialRef: null, retentionDays: null, region: null, rawPayloadAccepted: false as const, productionEventsAccepted: false as const, exportEnabled: false as const })));
export function validateStagingTelemetrySinks(sinks: readonly StagingTelemetrySink[]) { const codes: string[] = []; if (new Set(sinks.map((item) => item.pipeline)).size !== 5 || sinks.length !== 5) codes.push("PIPELINE_COVERAGE_REQUIRED"); if (sinks.some((item) => item.rawPayloadAccepted || item.productionEventsAccepted || item.exportEnabled)) codes.push("UNAUTHORIZED_EXPORT_OR_RAW_PAYLOAD"); const configured = sinks.filter((item) => item.endpointRef || item.credentialRef || item.retentionDays !== null || item.region); if (configured.some((item) => !item.endpointRef || !item.credentialRef || item.retentionDays === null || !item.region)) codes.push("PARTIAL_SINK_CONFIGURATION_FORBIDDEN"); return Object.freeze({ valid: codes.length === 0, codes: Object.freeze(codes), telemetryExportAuthorized: false as const }); }
