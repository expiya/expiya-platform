export type PilotOnboardingStage="INVITED"|"COMPANY_REVIEW"|"CONTRACTING"|"SANDBOX_TRAINING"|"DRY_RUN_IMPORT"|"OPERATIONAL_REVIEW"|"COHORT_READY"|"PAUSED"|"EXITED";
const transitions:Readonly<Record<PilotOnboardingStage,readonly PilotOnboardingStage[]>>={INVITED:["COMPANY_REVIEW","EXITED"],COMPANY_REVIEW:["CONTRACTING","PAUSED","EXITED"],CONTRACTING:["SANDBOX_TRAINING","PAUSED","EXITED"],SANDBOX_TRAINING:["DRY_RUN_IMPORT","PAUSED","EXITED"],DRY_RUN_IMPORT:["OPERATIONAL_REVIEW","SANDBOX_TRAINING","PAUSED"],OPERATIONAL_REVIEW:["COHORT_READY","DRY_RUN_IMPORT","PAUSED"],COHORT_READY:["PAUSED","EXITED"],PAUSED:["COMPANY_REVIEW","EXITED"],EXITED:[]};
export function canTransitionPilotOnboarding(from:PilotOnboardingStage,to:PilotOnboardingStage):boolean{return transitions[from].includes(to);}
export interface CohortReadinessInput {readonly stage:PilotOnboardingStage;readonly namedOwnerPresent:boolean;readonly trainedUsers:number;readonly dryRunRows:number;readonly dryRunErrorRatio:number;readonly supportChannelReady:boolean;readonly realDataTransferAuthorized:boolean}
export function assessCohortReadiness(input:CohortReadinessInput){const codes:string[]=[];if(input.stage!=="OPERATIONAL_REVIEW")codes.push("WRONG_STAGE");if(!input.namedOwnerPresent)codes.push("OWNER_REQUIRED");if(input.trainedUsers<2)codes.push("TRAINED_USERS_INSUFFICIENT");if(input.dryRunRows<10)codes.push("DRY_RUN_SAMPLE_TOO_SMALL");if(input.dryRunErrorRatio>.05)codes.push("DRY_RUN_ERROR_RATE_HIGH");if(!input.supportChannelReady)codes.push("SUPPORT_NOT_READY");if(input.realDataTransferAuthorized)codes.push("REAL_DATA_TRANSFER_PREMATURE");return Object.freeze({ready:codes.length===0,realDataTransferAuthorized:false as const,codes:Object.freeze(codes)});}
