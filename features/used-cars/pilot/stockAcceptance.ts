export interface PilotStockCandidate { readonly taxonomyEntityId:string|null; readonly taxonomyReleaseVersion:string|null; readonly vinFingerprintPresent:boolean; readonly plateFingerprintPresent:boolean; readonly branchVerified:boolean; readonly priceUpdatedAt:string; readonly stockUpdatedAt:string; readonly requiredFieldsComplete:boolean; readonly mediaPassed:boolean; readonly evidenceConflictsOpen:boolean; readonly duplicateSignals:readonly string[]; readonly classicVehicle:boolean }
export type StockPilotGateCode="TAXONOMY_IDENTITY_REQUIRED"|"TAXONOMY_VERSION_REQUIRED"|"PRIVATE_IDENTIFIER_REQUIRED"|"BRANCH_UNVERIFIED"|"STALE_PRICE"|"STALE_STOCK"|"REQUIRED_FIELDS_MISSING"|"MEDIA_NOT_CLEARED"|"EVIDENCE_CONFLICT"|"DUPLICATE_SIGNAL"|"CLASSIC_OUT_OF_SCOPE";
export function evaluatePilotStock(input:{readonly candidate:PilotStockCandidate;readonly now:string}):{readonly accepted:boolean;readonly publishable:false;readonly codes:readonly StockPilotGateCode[]}{const c=input.candidate,codes:StockPilotGateCode[]=[];const age=(value:string)=>Date.parse(input.now)-Date.parse(value);
 if(!c.taxonomyEntityId)codes.push("TAXONOMY_IDENTITY_REQUIRED");if(!c.taxonomyReleaseVersion)codes.push("TAXONOMY_VERSION_REQUIRED");if(!c.vinFingerprintPresent||!c.plateFingerprintPresent)codes.push("PRIVATE_IDENTIFIER_REQUIRED");if(!c.branchVerified)codes.push("BRANCH_UNVERIFIED");if(!Number.isFinite(age(c.priceUpdatedAt))||age(c.priceUpdatedAt)>7*86400000)codes.push("STALE_PRICE");if(!Number.isFinite(age(c.stockUpdatedAt))||age(c.stockUpdatedAt)>48*3600000)codes.push("STALE_STOCK");if(!c.requiredFieldsComplete)codes.push("REQUIRED_FIELDS_MISSING");if(!c.mediaPassed)codes.push("MEDIA_NOT_CLEARED");if(c.evidenceConflictsOpen)codes.push("EVIDENCE_CONFLICT");if(c.duplicateSignals.length>0)codes.push("DUPLICATE_SIGNAL");if(c.classicVehicle)codes.push("CLASSIC_OUT_OF_SCOPE");
 return Object.freeze({accepted:codes.length===0,publishable:false,codes:Object.freeze(codes)});}
