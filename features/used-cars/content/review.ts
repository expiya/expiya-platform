export interface PublicContentArtifact { readonly contentId: string; readonly locale: "tr-TR"; readonly version: string; readonly checksum: string; readonly contexts: readonly string[]; readonly contentDesignerId: string | null; readonly legalReviewerId: string | null; readonly accessibilityReviewerId: string | null; readonly evidenceReviewerId: string | null; readonly reviewedAt: string | null; readonly expiresAt: string | null; readonly supersededAt: string | null }
export function validatePublicContentReview(artifact: PublicContentArtifact, now: string) { const codes: string[] = []; if (!/^sha256:[a-f0-9]{64}$/u.test(artifact.checksum)) codes.push("INVALID_CHECKSUM"); const reviewers = [artifact.contentDesignerId, artifact.legalReviewerId, artifact.accessibilityReviewerId, artifact.evidenceReviewerId]; if (reviewers.some((reviewer) => !reviewer)) codes.push("REVIEW_MATRIX_INCOMPLETE"); if (new Set(reviewers.filter(Boolean)).size < 4) codes.push("REVIEWER_SEPARATION_REQUIRED"); if (!artifact.reviewedAt) codes.push("REVIEW_TIME_REQUIRED"); if (artifact.expiresAt && artifact.expiresAt <= now) codes.push("CONTENT_REVIEW_EXPIRED"); if (artifact.supersededAt) codes.push("CONTENT_SUPERSEDED"); return Object.freeze({ valid: codes.length === 0, codes: Object.freeze(codes), publicationAuthorized: false as const }); }
