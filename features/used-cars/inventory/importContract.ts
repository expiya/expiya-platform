export type InventoryImportMode="VALIDATE_ONLY"|"UPSERT_EXPLICIT"|"SNAPSHOT_RECONCILE";
export type InventoryImportSource="CSV"|"XLSX"|"SFTP_FEED"|"PULL_API"|"PUSH_API";
export interface InventoryImportEnvelope {readonly schemaVersion:"used-inventory-feed/v1";readonly importId:string;readonly tenantId:string;readonly branchId:string;readonly source:InventoryImportSource;readonly mode:InventoryImportMode;readonly taxonomyReleaseVersion:string;readonly sourceChecksum:string;readonly idempotencyKey:string;readonly generatedAt:string;readonly rows:readonly unknown[];readonly deletionByOmissionAllowed:false;readonly writeAuthorized:false}
export function validateImportEnvelope(envelope:InventoryImportEnvelope):readonly string[]{const codes:string[]=[];if(!envelope.importId||!envelope.tenantId||!envelope.branchId)codes.push("IMPORT_SCOPE_REQUIRED");if(!/^sha256:[a-f0-9]{64}$/u.test(envelope.sourceChecksum))codes.push("SOURCE_CHECKSUM_INVALID");if(!/^tr-used-pilot-\d+\.\d+\.\d+$/u.test(envelope.taxonomyReleaseVersion))codes.push("TAXONOMY_RELEASE_INVALID");if(envelope.rows.length===0)codes.push("EMPTY_IMPORT");if(envelope.rows.length>10_000)codes.push("IMPORT_TOO_LARGE");if(envelope.deletionByOmissionAllowed!==false)codes.push("DELETION_BY_OMISSION_FORBIDDEN");if(envelope.writeAuthorized!==false)codes.push("WRITE_AUTHORIZATION_FORBIDDEN");return Object.freeze(codes);}
