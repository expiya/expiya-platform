export type SellerAgentCapability="EXPLAIN_PUBLIC_LISTING"|"EXPLAIN_EVIDENCE_STATUS"|"ASK_USAGE_NEEDS"|"SCHEDULE_APPOINTMENT"|"REQUEST_HUMAN_HANDOFF"|"PRESENT_POLICY_OFFER";
export interface SellerAgentMandate {readonly mandateId:string;readonly tenantId:string;readonly branchId:string;readonly inventoryUnitId:string;readonly listingRevisionId:string;readonly capabilities:readonly SellerAgentCapability[];readonly issuedByActorId:string;readonly secondApproverActorId:string|null;readonly policyVersion:string;readonly startsAt:string;readonly expiresAt:string;readonly revokedAt:string|null;readonly aiDisclosureRequired:true;readonly bindingContractAllowed:false;readonly paymentCollectionAllowed:false;readonly hiddenFloorPriceDisclosureAllowed:false;readonly purchaseInstructionAllowed:false}
export type AgentAction="DESCRIBE_VEHICLE"|"CLAIM_VERIFIED_FACT"|"SCHEDULE"|"OFFER"|"ACCEPT_BINDING_SALE"|"COLLECT_PAYMENT"|"DISCLOSE_FLOOR_PRICE"|"SAY_BUY_OR_DONT_BUY";
export function authorizeSellerAgentAction(input:{readonly mandate:SellerAgentMandate;readonly action:AgentAction;readonly now:string;readonly aiDisclosed:boolean;readonly evidenceVerified:boolean}){const m=input.mandate;if(m.revokedAt)return {allowed:false,reason:"MANDATE_REVOKED"};if(input.now<m.startsAt||input.now>=m.expiresAt)return {allowed:false,reason:"MANDATE_INACTIVE"};if(!input.aiDisclosed)return {allowed:false,reason:"AI_DISCLOSURE_REQUIRED"};if(input.action==="ACCEPT_BINDING_SALE")return {allowed:false,reason:"BINDING_SALE_FORBIDDEN"};if(input.action==="COLLECT_PAYMENT")return {allowed:false,reason:"PAYMENT_FORBIDDEN"};if(input.action==="DISCLOSE_FLOOR_PRICE")return {allowed:false,reason:"SECRET_DISCLOSURE_FORBIDDEN"};if(input.action==="SAY_BUY_OR_DONT_BUY")return {allowed:false,reason:"PURCHASE_INSTRUCTION_FORBIDDEN"};if(input.action==="CLAIM_VERIFIED_FACT"&&!input.evidenceVerified)return {allowed:false,reason:"VERIFIED_EVIDENCE_REQUIRED"};const capability:Partial<Record<AgentAction,SellerAgentCapability>>={DESCRIBE_VEHICLE:"EXPLAIN_PUBLIC_LISTING",CLAIM_VERIFIED_FACT:"EXPLAIN_EVIDENCE_STATUS",SCHEDULE:"SCHEDULE_APPOINTMENT",OFFER:"PRESENT_POLICY_OFFER"};const required=capability[input.action];if(required&&!m.capabilities.includes(required))return {allowed:false,reason:"CAPABILITY_NOT_GRANTED"};if(input.action==="OFFER"&&!m.secondApproverActorId)return {allowed:false,reason:"SECOND_APPROVAL_REQUIRED"};return {allowed:true};}
