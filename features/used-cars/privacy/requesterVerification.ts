export interface PrivacyRequesterVerification {readonly requestId:string;readonly requesterType:"SUBJECT"|"AUTHORIZED_REPRESENTATIVE";readonly contactChallengeVerified:boolean;readonly accountSessionVerified:boolean;readonly proportionalAdditionalCheckPassed:boolean;readonly representativeAuthorityVerified:boolean;readonly requestedTenantIds:readonly string[];readonly discoveredRecipientTenantIds:readonly string[];readonly crossTenantSearchAuthorized:false;readonly verificationExpiresAt:string}
export function evaluateRequesterVerification(input:{readonly verification:PrivacyRequesterVerification;readonly now:string}){const v=input.verification,codes:string[]=[];if(input.now>=v.verificationExpiresAt)codes.push("VERIFICATION_EXPIRED");if(!v.contactChallengeVerified)codes.push("CONTACT_CHALLENGE_REQUIRED");if(!v.accountSessionVerified&&!v.proportionalAdditionalCheckPassed)codes.push("IDENTITY_ASSURANCE_INSUFFICIENT");if(v.requesterType==="AUTHORIZED_REPRESENTATIVE"&&!v.representativeAuthorityVerified)codes.push("REPRESENTATIVE_AUTHORITY_REQUIRED");if(v.requestedTenantIds.some(id=>!v.discoveredRecipientTenantIds.includes(id)))codes.push("TENANT_SCOPE_UNVERIFIED");if(v.crossTenantSearchAuthorized!==false)codes.push("CROSS_TENANT_SEARCH_FORBIDDEN");return Object.freeze({verified:codes.length===0,scopedTenantIds:Object.freeze(v.requestedTenantIds.filter(id=>v.discoveredRecipientTenantIds.includes(id))),codes:Object.freeze(codes)});}
