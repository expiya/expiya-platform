export type PrivacyRight="ACCESS"|"CORRECTION"|"DELETION"|"PROCESSING_OBJECTION"|"CONSENT_WITHDRAWAL"|"TRANSFER_INFORMATION";
export type PrivacyRequestStatus="RECEIVED"|"IDENTITY_PENDING"|"VERIFIED"|"SCOPING"|"LEGAL_REVIEW"|"FULFILLMENT"|"RESPONSE_READY"|"COMPLETED"|"REJECTED"|"WITHDRAWN";
const transitions:Readonly<Record<PrivacyRequestStatus,readonly PrivacyRequestStatus[]>>={RECEIVED:["IDENTITY_PENDING","WITHDRAWN"],IDENTITY_PENDING:["VERIFIED","REJECTED","WITHDRAWN"],VERIFIED:["SCOPING","WITHDRAWN"],SCOPING:["LEGAL_REVIEW","FULFILLMENT"],LEGAL_REVIEW:["FULFILLMENT","REJECTED"],FULFILLMENT:["RESPONSE_READY"],RESPONSE_READY:["COMPLETED"],COMPLETED:[],REJECTED:[],WITHDRAWN:[]};
export interface PrivacyRightsRequest {readonly requestId:string;readonly rights:readonly PrivacyRight[];readonly status:PrivacyRequestStatus;readonly receivedAt:string;readonly statutoryDueAt:string;readonly subjectScopeFingerprint:string;readonly requesterType:"SUBJECT"|"AUTHORIZED_REPRESENTATIVE";readonly identityEvidenceStored:false;readonly rawPiiInAudit:false;readonly automatedFulfillmentAuthorized:false}
export function canTransitionPrivacyRequest(from:PrivacyRequestStatus,to:PrivacyRequestStatus):boolean{return transitions[from].includes(to);}
export function validatePrivacyRightsRequest(request:PrivacyRightsRequest):readonly string[]{const codes:string[]=[];if(request.rights.length===0)codes.push("RIGHT_REQUIRED");if(request.statutoryDueAt<=request.receivedAt)codes.push("DUE_DATE_INVALID");if(!/^hmac-sha256:[^:]+:[a-f0-9]{64}$/u.test(request.subjectScopeFingerprint))codes.push("SUBJECT_FINGERPRINT_INVALID");if(request.identityEvidenceStored!==false)codes.push("IDENTITY_EVIDENCE_STORAGE_FORBIDDEN");if(request.rawPiiInAudit!==false)codes.push("RAW_PII_AUDIT_FORBIDDEN");if(request.automatedFulfillmentAuthorized!==false)codes.push("AUTOMATED_FULFILLMENT_FORBIDDEN");return Object.freeze(codes);}
