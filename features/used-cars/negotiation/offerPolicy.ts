export interface OfferPolicyEnvelope {readonly mandateId:string;readonly tenantId:string;readonly inventoryUnitId:string;readonly listingRevisionId:string;readonly policyVersion:string;readonly allowedAmountMinor:readonly [number,number];readonly currency:"TRY";readonly maximumRounds:number;readonly humanApprovalBelowMinor:number;readonly expiresAt:string}
export interface OfferRequest {readonly requestedAmountMinor:number;readonly round:number;readonly now:string}
export function evaluateOfferRequest(policy:OfferPolicyEnvelope,request:OfferRequest){if(request.now>=policy.expiresAt)return {allowed:false,reason:"POLICY_EXPIRED",humanApprovalRequired:true};if(request.round<1||request.round>policy.maximumRounds)return {allowed:false,reason:"ROUND_LIMIT",humanApprovalRequired:true};if(request.requestedAmountMinor<policy.allowedAmountMinor[0]||request.requestedAmountMinor>policy.allowedAmountMinor[1])return {allowed:false,reason:"OUTSIDE_POLICY_ENVELOPE",humanApprovalRequired:true};return {allowed:true,humanApprovalRequired:request.requestedAmountMinor<policy.humanApprovalBelowMinor,offerBinding:false as const};}
