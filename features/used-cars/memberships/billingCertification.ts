export type BillingCertificationScenario = "HOSTED_CHECKOUT_NO_CARD_DATA" | "SIGNED_WEBHOOK" | "REPLAY_REJECTED" | "IDEMPOTENCY_CONFLICT" | "AMOUNT_CURRENCY_MISMATCH" | "INVOICE_TENANT_ISOLATION" | "E_INVOICE_FAILURE" | "FULL_REFUND" | "PARTIAL_REFUND" | "CHARGEBACK_DISPUTE" | "CANCELLATION_EXPIRY" | "WEBHOOK_SECRET_ROTATION";
export const requiredBillingCertificationScenarios: readonly BillingCertificationScenario[] = Object.freeze(["HOSTED_CHECKOUT_NO_CARD_DATA", "SIGNED_WEBHOOK", "REPLAY_REJECTED", "IDEMPOTENCY_CONFLICT", "AMOUNT_CURRENCY_MISMATCH", "INVOICE_TENANT_ISOLATION", "E_INVOICE_FAILURE", "FULL_REFUND", "PARTIAL_REFUND", "CHARGEBACK_DISPUTE", "CANCELLATION_EXPIRY", "WEBHOOK_SECRET_ROTATION"]);
export interface BillingCertificationResult { readonly scenario: BillingCertificationScenario; readonly environment: "STAGING"; readonly syntheticOnly: true; readonly passed: boolean; readonly cardDataObservedByExpiya: false; readonly rawBillingPiiInEvidence: false; readonly findingsClosed: boolean; readonly reviewerIds: readonly string[]; readonly evidenceChecksum: string }
export function assessBillingCertification(results: readonly BillingCertificationResult[]) {
  const missing = requiredBillingCertificationScenarios.filter((scenario) => !results.some((result) => result.scenario === scenario && result.environment === "STAGING" && result.syntheticOnly && result.passed && !result.cardDataObservedByExpiya && !result.rawBillingPiiInEvidence && result.findingsClosed && new Set(result.reviewerIds).size >= 2 && /^sha256:[a-f0-9]{64}$/u.test(result.evidenceChecksum)));
  return Object.freeze({ complete: missing.length === 0, missing: Object.freeze(missing), realChargeAuthorized: false as const, invoiceIssuanceAuthorized: false as const });
}
