export interface SecretInventoryEntry { readonly secretId: string; readonly environment: "STAGING" | "PRODUCTION"; readonly purpose: string; readonly ownerId: string | null; readonly kmsReference: string | null; readonly lastRotatedAt: string | null; readonly nextRotationAt: string | null; readonly repositoryStored: false; readonly sharedAcrossPublicPartnerOps: false }
export function validateSecretInventory(entries: readonly SecretInventoryEntry[], now: string) { const codes: string[] = []; if (new Set(entries.map((entry) => `${entry.environment}:${entry.secretId}`)).size !== entries.length) codes.push("DUPLICATE_SECRET_ID"); for (const entry of entries) { if (!entry.ownerId || !entry.kmsReference) codes.push(`OWNERSHIP_OR_KMS_MISSING:${entry.secretId}`); if (!entry.lastRotatedAt || !entry.nextRotationAt || entry.nextRotationAt <= now) codes.push(`ROTATION_DUE:${entry.secretId}`); if (entry.repositoryStored !== false) codes.push(`REPOSITORY_SECRET_FORBIDDEN:${entry.secretId}`); if (entry.sharedAcrossPublicPartnerOps !== false) codes.push(`CROSS_SURFACE_SECRET_FORBIDDEN:${entry.secretId}`); } return Object.freeze({ valid: codes.length === 0, codes: Object.freeze(codes), secretActivationAuthorized: false as const }); }
