export interface ApiDeprecationNotice { readonly endpointId: string; readonly currentVersion: string; readonly replacementVersion: string; readonly announcedAt: string; readonly sunsetAt: string; readonly migrationGuideChecksum: string; readonly affectedTenantNotificationComplete: boolean; readonly usageBelowThreshold: boolean; readonly securityEmergency: boolean }
export function validateApiDeprecation(notice: ApiDeprecationNotice) { const codes: string[] = []; const minimumWindowMs = 90 * 86400000; if (notice.replacementVersion === notice.currentVersion) codes.push("REPLACEMENT_VERSION_REQUIRED"); if (!notice.securityEmergency && Date.parse(notice.sunsetAt) - Date.parse(notice.announcedAt) < minimumWindowMs) codes.push("DEPRECATION_WINDOW_TOO_SHORT"); if (!/^sha256:[a-f0-9]{64}$/u.test(notice.migrationGuideChecksum)) codes.push("MIGRATION_GUIDE_REQUIRED"); if (!notice.affectedTenantNotificationComplete) codes.push("TENANT_NOTIFICATION_REQUIRED"); if (!notice.usageBelowThreshold) codes.push("ACTIVE_USAGE_REMAINS"); return Object.freeze({ readyToSunset: codes.length === 0, codes: Object.freeze(codes), automaticSunsetAuthorized: false as const }); }
