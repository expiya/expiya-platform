export interface ApiErrorEnvelope { readonly version: "used-cars-api-error/v1"; readonly requestId: string; readonly code: string; readonly message: string; readonly fieldErrors: readonly { field: string; code: string }[]; readonly retryable: boolean; readonly retryAfterSeconds: number | null; readonly internalDetailsIncluded: false }
export function validateApiErrorEnvelope(error: ApiErrorEnvelope) { const codes: string[] = []; if (!error.requestId || !/^[A-Z0-9_:-]+$/u.test(error.code)) codes.push("CORRELATION_AND_CODE_REQUIRED"); if (error.retryable !== (error.retryAfterSeconds !== null)) codes.push("RETRY_CONTRACT_MISMATCH"); if (error.internalDetailsIncluded !== false) codes.push("INTERNAL_DETAILS_FORBIDDEN"); if (error.fieldErrors.some((item) => !item.field || !item.code)) codes.push("INVALID_FIELD_ERROR"); return Object.freeze(codes); }
export interface CursorPage<T> { readonly version: "used-cars-cursor-page/v1"; readonly items: readonly T[]; readonly nextCursor: string | null; readonly snapshotAt: string; readonly totalCountExcluded: true }
export function validateCursorPage<T>(page: CursorPage<T>) { const codes: string[] = []; if (page.items.length > 100) codes.push("PAGE_SIZE_EXCEEDED"); if (page.nextCursor !== null && !/^[A-Za-z0-9_-]{16,512}$/u.test(page.nextCursor)) codes.push("INVALID_CURSOR"); if (page.totalCountExcluded !== true) codes.push("TOTAL_COUNT_ENUMERATION_FORBIDDEN"); return Object.freeze(codes); }
