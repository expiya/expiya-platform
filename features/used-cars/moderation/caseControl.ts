export interface ModerationCaseControl {readonly caseId:string;readonly tenantId:string;readonly subjectRevisionId:string;readonly assignedActorId:string;readonly assignedTeam:"MODERATION"|"TRUST_SAFETY"|"CLASSIC_SPECIALIST";readonly firstDecisionActorId:string|null;readonly secondReviewerActorId:string|null;readonly evidenceSnapshotChecksum:string;readonly expiresAt:string;readonly status:"ASSIGNED"|"FIRST_DECISION"|"SECOND_REVIEW"|"CLOSED"}
export type CaseControlCode="ASSIGNMENT_MISMATCH"|"ASSIGNMENT_EXPIRED"|"REVISION_MISMATCH"|"EVIDENCE_SNAPSHOT_INVALID"|"SECOND_REVIEW_REQUIRED"|"SAME_ACTOR_SECOND_REVIEW";
export function evaluateCaseDecision(input:{readonly control:ModerationCaseControl;readonly actorId:string;readonly subjectRevisionId:string;readonly now:string;readonly highImpact:boolean}):{readonly allowed:boolean;readonly codes:readonly CaseControlCode[]}{const c=input.control,codes:CaseControlCode[]=[];if(c.assignedActorId!==input.actorId&&c.secondReviewerActorId!==input.actorId)codes.push("ASSIGNMENT_MISMATCH");if(input.now>=c.expiresAt)codes.push("ASSIGNMENT_EXPIRED");if(c.subjectRevisionId!==input.subjectRevisionId)codes.push("REVISION_MISMATCH");if(!/^sha256:[a-f0-9]{64}$/u.test(c.evidenceSnapshotChecksum))codes.push("EVIDENCE_SNAPSHOT_INVALID");if(input.highImpact&&!c.secondReviewerActorId)codes.push("SECOND_REVIEW_REQUIRED");if(c.firstDecisionActorId&&c.secondReviewerActorId===c.firstDecisionActorId)codes.push("SAME_ACTOR_SECOND_REVIEW");return Object.freeze({allowed:codes.length===0,codes:Object.freeze(codes)});}
