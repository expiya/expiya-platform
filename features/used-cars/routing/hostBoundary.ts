export type AppHost="PLATFORM"|"PARTNER"|"UNKNOWN";
export type AppSurface="PUBLIC_USED_CARS"|"PARTNER_APP"|"PARTNER_DEMO"|"OTHER";
export function classifyAppHost(host:string):AppHost {const normalized=host.toLowerCase().split(":")[0];if(normalized==="expiya.com"||normalized==="www.expiya.com")return "PLATFORM";if(normalized==="partner.expiya.com")return "PARTNER";return "UNKNOWN";}
export function authorizeHostSurface(input:{readonly host:string;readonly pathname:string;readonly production:boolean}):{readonly allowed:boolean;readonly surface:AppSurface;readonly reason?:string}{const host=classifyAppHost(input.host);const publicPath=input.pathname==="/ikinciel"||input.pathname.startsWith("/ikinciel/")||input.pathname==="/cars/ikinciel"||input.pathname.startsWith("/cars/ikinciel/");const demo=input.pathname==="/ikinciel/partner-demo"||input.pathname.startsWith("/ikinciel/partner-demo/");if(host==="UNKNOWN")return {allowed:false,surface:"OTHER",reason:"UNTRUSTED_HOST"};if(demo&&input.production)return {allowed:false,surface:"PARTNER_DEMO",reason:"PARTNER_DEMO_FORBIDDEN_IN_PRODUCTION"};if(host==="PLATFORM"&&publicPath&&!demo)return {allowed:true,surface:"PUBLIC_USED_CARS"};if(host==="PARTNER"&&!publicPath)return {allowed:true,surface:"PARTNER_APP"};return {allowed:false,surface:demo?"PARTNER_DEMO":"OTHER",reason:"HOST_SURFACE_MISMATCH"};}
export const applicationIsolation=Object.freeze({sharedSessionCookie:false as const,sharedCsrfSecret:false as const,sharedAuthAudience:false as const,partnerDatabaseRoleDistinct:true as const,publicDatabaseRoleReadOnly:true as const,operationsPlaneDistinct:true as const});
