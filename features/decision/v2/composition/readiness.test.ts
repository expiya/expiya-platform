import { describe, expect, it } from "vitest";
import { assessCarsDecisionV2ProductionReadiness } from "./readiness.server";
describe("V2 production readiness gate", () => { const readyInput = { catalog: { status: "READY", snapshot: {} } as never, dailyLifeReady: true, personaReady: true, personaApproved: true, providerConfigured: true, durableStoreConfigured: true, signingSecretValid: true, migrationAvailable: true, publicFlag: true }; it("requires every production dependency", () => expect(assessCarsDecisionV2ProductionReadiness(readyInput)).toEqual({ ready: true, failures: [] })); it("preserves temporal and public blockers", () => { const result = assessCarsDecisionV2ProductionReadiness({ ...readyInput, catalog: { status: "UNAVAILABLE", reason: "NOT_YET_EFFECTIVE", diagnostics: [] }, publicFlag: false }); expect(result.ready).toBe(false); expect(result.failures).toEqual(expect.arrayContaining(["CATALOG_NOT_YET_EFFECTIVE", "PUBLIC_FLAG_DISABLED"])); }); });
