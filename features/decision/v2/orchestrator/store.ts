import type { StoredV2Conversation, V2ConversationStore } from "./types";
export class InMemoryV2ConversationStore implements V2ConversationStore { private readonly records = new Map<string, StoredV2Conversation>(); async load(id: string) { const value = this.records.get(id); return value ? structuredClone(value) : null; } async commit(input: { expectedRevision: number; next: StoredV2Conversation }) { const current = this.records.get(input.next.conversationId); if ((current?.revision ?? 0) !== input.expectedRevision) return { status: "REVISION_CONFLICT" as const }; this.records.set(input.next.conversationId, structuredClone(input.next)); return { status: "OK" as const }; } }
