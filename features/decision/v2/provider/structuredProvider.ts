import type { StructuredInterpretationModel, StructuredInterpretationRequest } from "../interpretation/types";
import type { NaturalRealizationModel, NaturalRealizationRequest } from "../realization/types";
export type ProviderErrorClass = "TIMEOUT" | "CANCELLED" | "RATE_LIMIT" | "UNAVAILABLE" | "INVALID_OUTPUT";
export interface StructuredProviderTransport { execute(input: { readonly task: "INTERPRET" | "REALIZE"; readonly payload: unknown; readonly signal: AbortSignal }): Promise<unknown> }
async function boundedCall(transport: StructuredProviderTransport, task: "INTERPRET" | "REALIZE", payload: unknown, timeoutMs: number, outer?: AbortSignal) { const controller = new AbortController(); const timeout = setTimeout(() => controller.abort("timeout"), timeoutMs); const abort = () => controller.abort("cancelled"); if (outer?.aborted) abort(); else outer?.addEventListener("abort", abort, { once: true }); try { return await transport.execute({ task, payload, signal: controller.signal }); } catch (error) { if (controller.signal.aborted) throw new Error(controller.signal.reason === "timeout" ? "V2_PROVIDER_TIMEOUT" : "V2_PROVIDER_CANCELLED"); throw error; } finally { clearTimeout(timeout); outer?.removeEventListener("abort", abort); } }
export function createStructuredProviderAdapters(input: { readonly transport: StructuredProviderTransport; readonly timeoutMs: number; readonly signal?: AbortSignal }): { readonly interpreter: StructuredInterpretationModel; readonly realizer: NaturalRealizationModel } { return Object.freeze({ interpreter: { interpret: (request: StructuredInterpretationRequest) => boundedCall(input.transport, "INTERPRET", request, input.timeoutMs, input.signal) }, realizer: { realize: (request: NaturalRealizationRequest) => boundedCall(input.transport, "REALIZE", request, input.timeoutMs, input.signal) } }); }
